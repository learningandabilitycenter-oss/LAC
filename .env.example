PAYLOAD_SECRET=replace-with-a-long-random-secret
DATABASE_URL=file:./lac-cms.db
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Production: replace DATABASE_URL with a Neon Postgres pooled connection string.
# Optional on Vercel: connect Blob storage to enable persistent CMS media uploads.
# BLOB_READ_WRITE_TOKEN=
